# 《库尔斯克会战浪尖战场》坦克对战游戏技术文档

> 版本：v1.1
> 修订：统一伤害口径、修正状态机与事件名、补全胜负判定边界与水印可见性。
> 文档类型：概要设计 + 详细设计
> 目标平台：PC 浏览器（兼容 Chrome / Edge / Firefox，预留移动端适配）

---

## 1. 项目概述

### 1.1 项目背景

本游戏以二战库尔斯克会战为题材背景，打造一款 3D 坦克对战射击游戏。玩家驾驶坦克在"浪尖战场"中与敌方坦克展开对战，通过移动、瞄准、射击击败所有敌方坦克获得胜利。

### 1.2 游戏名称

- **主页标题**：库尔斯克会战浪尖战场
- **战场水印**：浪尖战场（平铺显示于游戏底图）

### 1.3 核心玩法一句话描述

> 玩家操控一辆坦克，在 1:2 长方形战场中利用障碍物作掩护，在弹药限制条件下击毁全部敌方坦克即胜利；血量归零，或弹药耗尽（弹药=0 且无己方炮弹在飞行）且场上仍有敌人则失败。

---

## 2. 需求规格

### 2.1 功能性需求清单

| 编号 | 需求 | 优先级 | 对应模块 | 对应用户要求 |
|------|------|--------|----------|--------------|
| FR-01 | 玩家坦克移动控制（前后、转向） | P0 | 玩家控制模块 | 用户要求 1 |
| FR-02 | 发射炮弹（含弹药量限制与换弹） | P0 | 武器系统 | 用户要求 1 |
| FR-03 | 瞄准镜开镜/关镜（FOV 切换 + 精准准星） | P0 | 相机系统 | 用户要求 1 |
| FR-04 | 敌方坦克 AI（巡逻、追击、射击） | P0 | AI 模块 | 用户要求 2 |
| FR-05 | 障碍物系统（掩体、不可摧毁物） | P0 | 碰撞/地图模块 | 用户要求 2 |
| FR-06 | 碰撞检测（坦克-坦克、炮弹-坦克、炮弹-障碍物、坦克-边界） | P0 | 物理模块 | 用户要求 2 |
| FR-07 | 胜负判定（击毁全部敌人 → 胜利；玩家血量归零 → 失败） | P0 | 游戏流程控制 | 用户要求 2 |
| FR-08 | 失败弹窗：文案"若是巅峰留不住，那就重走来时路" + "返回主页"/"重新挑战"按钮 | P0 | UI 模块 | 用户要求 4 |
| FR-09 | 胜利横幅：全屏横幅显示"胜利" | P0 | UI 模块 | 用户要求 4 |
| FR-10 | HUD：弹药量、击毁坦克数、坦克血条 | P0 | HUD 模块 | 用户要求 6 |
| FR-11 | 底图水印"浪尖战场" | P0 | 渲染模块 | 用户要求 5 |
| FR-12 | 3D 立体地图（1:2 平坦矩形战场） | P0 | 地图模块 | 用户要求 3、7 |
| FR-13 | 操作反馈（开炮后坐力、受击闪红、命中特效、音效） | P0 | 反馈模块 | 用户要求 3 |

### 2.2 非功能性需求

| 编号 | 需求 |
|------|------|
| NFR-01 | 帧率目标 60 FPS（1080P，中端显卡） |
| NFR-02 | 首屏加载 ≤ 5 秒（资源本地化） |
| NFR-03 | 所有 UI 中文显示，界面布局清晰无遮挡 |
| NFR-04 | 键鼠操作为主，操作即时响应（输入延迟 ≤ 50ms 感知） |

---

## 3. 技术选型

| 类别 | 方案 | 说明 |
|------|------|------|
| 渲染引擎 | **Three.js (r160+)** | 基于 WebGL，生态成熟，满足 3D 场景需求 |
| 开发语言 | JavaScript (ES6+, ES Module) | 团队熟悉 JS，可直接用 ES Module |
| 物理与碰撞 | **自研轻量 AABB/球体碰撞** | 坦克对战场景简单，无需引入 Cannon/Ammo 降低复杂度 |
| UI 系统 | HTML + CSS（DOM 叠加层） | HUD、弹窗、横幅均用 DOM 实现，不占用 WebGL 上下文 |
| 构建工具 | Vite | 本地开发热更新，产物静态化 |
| 音效 | Web Audio API | 炮击、命中、引擎声 |

> 选型理由：用户技术栈为 HTML/CSS/JS + WebGL（Shader 熟悉），Three.js 最贴合；碰撞场景为规则几何体（矩形战场 + 盒状障碍物 + 坦克包围盒），自研碰撞比物理引擎更可控、更轻量。

---

## 4. 系统架构

### 4.1 总体架构图

```
┌─────────────────────────────────────────────────────────┐
│                      UI 层 (DOM/CSS)                     │
│  主页 │ HUD(弹药/击杀/血条) │ 失败弹窗 │ 胜利横幅 │ 水印层  │
├─────────────────────────────────────────────────────────┤
│                     游戏逻辑层 (JS)                      │
│  游戏状态机 │ 玩家控制 │ AI控制器 │ 武器系统 │ 胜负判定     │
├─────────────────────────────────────────────────────────┤
│                     渲染与物理层                          │
│  Three.js 场景 │ 相机系统 │ 碰撞检测 │ 粒子特效 │ 音效     │
├─────────────────────────────────────────────────────────┤
│                     资源层                               │
│  模型(Geometry/材质) │ 地面贴图(含水印) │ 音频 │ 配置表     │
└─────────────────────────────────────────────────────────┘
```

### 4.2 目录结构规划

```
tank-battle/
├── index.html              # 入口（主页 + 游戏容器）
├── src/
│   ├── main.js             # 启动入口
│   ├── config.js           # 全局配置（战场尺寸、坦克数值、弹药数）
│   ├── core/
│   │   ├── GameStateMachine.js   # 游戏状态机
│   │   └── EventBus.js           # 事件总线（解耦逻辑与UI）
│   ├── scene/
│   │   ├── SceneManager.js       # Three.js 场景/渲染器管理
│   │   ├── Battlefield.js        # 战场（地面+边界+水印）
│   │   ├── Obstacles.js          # 障碍物生成与管理
│   │   └── Lighting.js           # 光照
│   ├── entities/
│   │   ├── Tank.js               # 坦克基类（模型+属性）
│   │   ├── PlayerTank.js         # 玩家坦克（受控）
│   │   ├── EnemyTank.js          # 敌方坦克（挂AI）
│   │   └── Projectile.js         # 炮弹
│   ├── systems/
│   │   ├── InputSystem.js        # 键鼠输入
│   │   ├── CameraSystem.js       # 第三人称/瞄准镜切换
│   │   ├── CollisionSystem.js    # 碰撞检测
│   │   ├── AISystem.js           # 敌方AI
│   │   ├── WeaponSystem.js       # 开炮/装填/弹药
│   │   └── FeedbackSystem.js     # 特效/音效/震动反馈
│   └── ui/
│       ├── HomePage.js           # 主页（游戏名+开始按钮）
│       ├── HUD.js                # 弹药/击杀/血条
│       ├── ResultUI.js           # 失败弹窗 / 胜利横幅
│       └── Watermark.js          # 水印层
├── assets/
│   ├── textures/            # 地面、水印纹理
│   └── audio/               # 音效文件
└── styles/
    └── main.css             # UI 样式
```

### 4.3 游戏状态机

```
                           ┌──────────┐
         ┌────────────────►│   HOME   │◄────────────────┐
         │                 └────┬─────┘                │
         │                      │ 点击"开始战斗"        │
         │                      ▼                      │
         │                 ┌──────────┐                │
         │          ┌─────►│ PLAYING  │◄─────┐         │
         │          │      └─┬──────┬─┘      │         │
         │          │        │      │        │         │
         │          │  击毁全部敌人│      │玩家HP≤0│        │
         │          │        │      │或弹尽(无己方│        │
         │          │        │      │炮弹在飞)且│        │
         │          │        │      │仍有敌人│        │
         │          │        ▼      ▼        │         │
         │          │   ┌───────┐┌───────┐   │         │
         │          │   │  WIN  ││  LOSE │   │         │
         │          │   │       ││       │   │         │
         │ 重新挑战 └───┤       ││       ├───┘ 重新挑战 │
         │              └─┬─────┘└─────┬─┘             │
         │                │            │               │
         │   点击"返回主页"│            │点击"返回主页"  │
         │                │            │               │
         └────────────────┘            └───────────────┘
```

> 转移说明：HOME --点击"开始战斗"--> PLAYING；PLAYING --击毁全部敌人--> WIN；PLAYING --玩家HP≤0 或 弹尽(且无己方炮弹在飞)且仍有敌人--> LOSE；WIN / LOSE --点击"重新挑战"--> PLAYING（重置后直接开局）；WIN / LOSE --点击"返回主页"--> HOME。

状态定义：

| 状态 | 说明 |
|------|------|
| `HOME` | 显示主页，标题"库尔斯克会战浪尖战场"，提供开始按钮 |
| `PLAYING` | 游戏进行中，接受输入、跑 AI、跑物理 |
| `WIN` | 触发胜利横幅，游戏逻辑暂停 |
| `LOSE` | 触发失败弹窗，游戏逻辑暂停 |

> 进入 `WIN` / `LOSE` 时清空按键缓存、强制退出开镜、释放指针锁定，避免重开后残留输入。

---

## 5. 3D 场景与地图设计

### 5.1 战场规格（FR-12）

| 参数 | 值 | 说明 |
|------|-----|------|
| 战场形状 | 长方形 | **边长比 1 : 2** |
| 具体尺寸 | **宽 100 单位 × 长 200 单位**（米） | 可在 config.js 调整，保持比例 |
| 地形 | 平坦（Flat） | 地面 Y=0 平面，无坡度无起伏 |
| 边界 | 战场四周设不可穿越边界（沙袋墙/铁丝网模型） | 坦克与炮弹均不可越界 |

坐标系约定：

- Three.js 世界坐标：X 轴 → 战场宽（-50 ~ +50），Z 轴 → 战场长（-100 ~ +100），Y 轴向上。
- 坦克贴地行驶，`y = 0`。

### 5.2 战场底图与水印（FR-11）

水印方案（双保险）：

1. **地面纹理水印（推荐）**：用 Canvas 动态生成地面贴图 —— 底色为土地/草地纹理，在其上以平铺方式绘制旋转 -30°、透明度 ≥0.08 的半透明文字"浪尖战场"，生成 `CanvasTexture` 铺满地面。优点：跟随 3D 地面透视，沉浸感强，无 DOM 开销。
2. **屏幕层水印（辅助）**：在游戏容器上叠加一层水印（`pointer-events: none`，DOM 平铺多个绝对定位 span 或平铺背景图），作为 HUD 之上的淡淡水印。用于确保任何视角下水印可见。

地面 Canvas 贴图生成伪代码：

```javascript
function createGroundTexture() {
  const canvas = document.createElement('canvas');
  canvas.width = 2048; canvas.height = 4096; // 与战场1:2对应
  const ctx = canvas.getContext('2d');
  // 1. 绘制底色：土黄/草绿混合噪声（模拟战场土地）
  drawTerrainBase(ctx);
  // 2. 平铺水印
  ctx.save();
  ctx.globalAlpha = 0.08;            // 透明度 ≥0.08：低可见但不消失
  ctx.font = 'bold 120px sans-serif';
  ctx.fillStyle = '#ffffff';
  ctx.rotate(-Math.PI / 6);          // 斜向水印
  for (let x = -2000; x < 3000; x += 600)
    for (let y = -2000; y < 5000; y += 400)
      ctx.fillText('浪尖战场', x, y);
  ctx.restore();
  return new THREE.CanvasTexture(canvas);
}
```

### 5.3 障碍物设计（FR-05）

| 障碍类型 | 是否可摧毁 | 尺寸(米) | 数量 | 作用 |
|----------|-----------|-----------|------|------|
| 岩石 | 否 | 4×3×4 | 8 | 掩体，阻挡炮弹与坦克 |
| 废弃碉堡 | 否 | 8×4×8 | 3 | 大型掩体 |
| 木箱 | 是（1发炮弹） | 2×2×2 | 12 | 可破坏掩体 |
| 燃料桶 | 是（爆炸伤害） | 半径1 | 6 | 爆炸波及附近坦克 |

障碍物布局原则：

- 对称或半随机（保证每局不同但公平），使用**固定随机种子**生成。
- 中线区域（Z ∈ [-20, 20]）障碍密度稍高，制造争夺区。
- 玩家出生点（Z = -85）与敌人出生区（Z = +80 附近）周围留 15 米净空。

### 5.4 光照与氛围

- 主光源：DirectionalLight（暖色，模拟午后阳光），开启阴影贴图（2048×2048）。
- 环境光：HemisphereLight（天空蓝 → 地面土色补光）。
- 雾效：轻微远距雾（Fog，颜色与天空一致），增强纵深感。
- 天空：渐变天空盒或纯色 + 云贴图。

---

## 6. 游戏实体设计

### 6.1 坦克（Tank 基类）

模型构成（Three.js Group）：

```
TankGroup
 ├── 车体 (BoxGeometry, 3.6×1.2×6)   ← 位置控制、移动
 ├── 履带×2 (Box, 两侧)
 ├── 炮塔 (CylinderGeometry, 水平旋转) ← 独立朝向，指向目标
 └── 炮管 (CylinderGeometry, 竖直俯仰)  ← 开炮动画后坐
```

属性表：

| 属性 | 玩家值 | 敌方值 | 说明 |
|------|--------|--------|------|
| maxHP | 100 | 60 | 血条上限 |
| 移动速度 | 12 m/s | 8 m/s | 前进/后退 |
| 转向速度 | 90°/s | 60°/s | 车体转向 |
| 炮塔转速 | 120°/s | 90°/s | 炮塔独立旋转 |
| 炮弹伤害 | 34（2发击毁普通敌人 / 3发击毁精英） | 25（4发击毁玩家） | 平衡值 |
| 射速 | 2.5 秒/发（装填） | 3.5 秒/发（精英 2.5 秒/发） | |
| 弹速 | 80 m/s | 60 m/s | |

> 数值集中于 `config.js`，便于平衡性调整。
> 击毁所需发数 = ceil(HP ÷ 伤害)。

### 6.2 炮弹（Projectile）

- 模型：小球体 + 发光材质 + 拖尾（可选简单粒子）。
- 生命周期：发射 → 直线飞行（本作不做弹道下坠，平射）→ 命中/超程后销毁。
- 超程保护：飞行超过 300 单位或 4 秒自动销毁。

### 6.3 敌方坦克配置

| 参数 | 值 |
|------|-----|
| 敌人数量 | 4 辆（普通×3 + 精英×1，精英血量 100、装填更快 2.5s） |
| 出生方式 | 开局在预设点位生成；本作不做刷新援军（可扩展波次） |

---

## 7. 核心系统详细设计

### 7.1 玩家控制系统（FR-01 / FR-02 / FR-03）

#### 7.1.1 键位表

| 按键 | 功能 |
|------|------|
| `W` / `S` | 前进 / 后退 |
| `A` / `D` | 车体左转 / 右转 |
| 鼠标移动 | 炮塔指向（水平）+ 炮管俯仰 |
| 鼠标左键 | 发射炮弹 |
| 鼠标右键（按住） | **开瞄准镜** |
| `R` | 手动装填（剩余弹药>0时） |

#### 7.1.2 移动逻辑

```
每帧（dt）：
  1. 读取 W/S → 计算目标速度 targetSpeed（+speed / -speed*0.6 / 0）
  2. 速度向 targetSpeed 平滑插值（模拟坦克惯性，加速度 20 m/s²）
  3. 读取 A/D → 车体绕 Y 轴旋转 rotSpeed*dt
  4. 坦克位置 += 前向向量 × 当前速度 × dt
  5. 碰撞系统检测：若与障碍物/坦克/边界相交 → 修正位置（推出碰撞盒）
  6. 行驶时播放引擎音效（音调随速度变化）、履带卷起尘土粒子
```

#### 7.1.3 瞄准镜设计（关键功能）

双相机模式切换：

| 模式 | 相机 | FOV | 准星 |
|------|------|-----|------|
| 第三人称 | 炮塔后上方跟随相机，平滑跟随 | 60° | 简易十字准星 |
| 瞄准镜 | 炮口末端第一人称相机 | **20°（放大）** | 战争游戏风格瞄准镜 UI（圆形遮罩+刻度线） |

- 按住右键开镜：相机瞬间切到炮管视角 + FOV 从 60 平滑过渡到 20（0.15s 内插值），屏幕叠加瞄准镜遮罩 UI（CSS 实现：黑色圆形挖空 + 十字线）。
- 松开右键：切回第三人称。
- 开镜时鼠标灵敏度自动降低 50%，便于精确瞄准。
- 开镜界面显示当前俯仰角度刻度（增强真实感与操作反馈）。

#### 7.1.4 射击逻辑

```
开火条件：弹药>0 且 装填完成 且 当前状态为PLAYING
流程：
  1. 从炮口位置沿炮管朝向生成 Projectile
  2. 弹药数 -1（HUD 闪烁提示）
  3. 触发装填计时（2.5s），HUD 装填进度条开始走
  4. 反馈：炮口火光粒子 + 炮管后坐动画（沿炮管方向回缩0.3m再复位）
         + 相机震屏(0.2s) + 开炮音效
```

弹药规则（配合 HUD FR-10）：

- 初始弹药：**20 发**。
- 击毁一辆敌方坦克：奖励弹药 +5（鼓励进攻，防止弹尽僵局）。
- 弹药 = 0 且无己方炮弹在飞行且场上仍有敌人 → 判负（最后一发命中优先于弹尽判负，见 7.5）。

### 7.2 敌方 AI 系统（FR-04）

采用简单实用的**行为状态机 + 感知系统**：

```
敌方AI状态机：
  PATROL（巡逻）──发现玩家──► ATTACK（攻击）
     ▲                        │  │
     │                   失去视野│  │玩家太近
     └──────────恢复──────────┘  ▼
                          COMBAT（绕行近战）
```

| 状态 | 行为 |
|------|------|
| `PATROL` | 沿预设路径点往返巡逻，炮塔缓慢扫视 |
| `ATTACK` | 停止移动或小幅侧移，炮塔追踪玩家，进入射程且装填完成即开炮 |
| `COMBAT` | 玩家距离 < 30m 时，环绕玩家移动（保持 ~25m 距离）并持续开炮 |

感知与决策：

```
每 0.2s 决策一次（错帧决策，省性能）：
  1. 视野检测：与玩家距离 < 120m 且 射线检测(Raycast)无障碍物遮挡 → 可见
  2. 可见 → 进入 ATTACK/COMBAT；连续 5 秒不可见 → 回 PATROL（走向玩家最后出现位置）
  3. 攻击时瞄准玩家位置 + 少量随机散布（命中精度随距离下降），避免"神枪手"体验
  4. 移动前检查路径：前方被障碍物阻挡 → 随机左/右偏转绕行（简单避障）
```

> 设计原则：AI 不追求拟人智能，追求"打得中但打得过"的爽快对轰体验。

### 7.3 碰撞检测系统（FR-06）

全部采用**轴对齐包围盒（AABB）+ 球体**两级检测，战场是平坦矩形、障碍物均为规则体，无需复杂物理：

| 碰撞对 | 检测方式 | 响应 |
|--------|----------|------|
| 坦克 vs 障碍物 | AABB vs AABB | 推出修正（沿最小穿透轴把坦克移出），坦克轻微减速 |
| 坦克 vs 坦克 | AABB vs AABB | 双方互相推开，均减速 |
| 坦克 vs 边界 | AABB vs 边界平面 | 位置钳制在战场范围内 |
| 炮弹 vs 坦克 | 球体 vs AABB | 坦克扣血、炮弹销毁、爆炸特效 |
| 炮弹 vs 障碍物 | 球体 vs AABB | 不可摧毁物：炮弹销毁+火花；可摧毁物：障碍物损毁 |
| 炮弹 vs 边界/地面 | 球体 vs 平面 | 炮弹销毁 |

帧循环内碰撞流程：

```
1. 收集本帧所有活动炮弹、所有坦克、所有障碍物
2. 炮弹逐帧移动后做连续碰撞（按步长细分，防止高速穿透）
3. 命中判定成功 → 通过 EventBus 广播事件：
   - `shell:hit:tank`（伤害、位置，载荷含 team 阵营字段）
   - `shell:hit:obstacle`
   - `tank:destroyed`（谁击毁了谁）
4. UI、特效、胜负判定模块订阅事件做出反应（解耦）
```

伤害与受击表现：

- 玩家受击：屏幕边缘红色泛光(Vignette) 0.3s + 相机震动 + HUD 血条抖动。
- 敌人受击：坦克模型闪白/闪红 0.1s（材质 emissive 调制）。
- 坦克被击毁：爆炸粒子（火光+烟尘）+ 残骸模型（焦黑坦克壳）留在原地（残骸成为新掩体，可用射线遮挡）。
- 燃料桶爆炸击杀敌人：计入玩家击毁数，并触发 +5 弹药奖励。

### 7.4 相机系统

| 相机 | 用途 | 参数 |
|------|------|------|
| 主相机 | 第三人称跟随 | 位于炮塔后上方 (0, 4, -8)（坦克局部系），lerp 平滑跟随，看向炮塔前方 |
| 瞄准镜相机 | 开镜 | 炮口位置，沿炮管朝向，FOV 20° |
| 主页相机 | 主页背景展示 | 环绕战场慢速旋转的展示相机 |

震屏实现：开炮/受击时给相机位置叠加衰减噪声（`offset = noise * intensity`，intensity 随时间指数衰减）。

### 7.5 胜负判定（FR-07）

| 结果 | 触发条件 |
|------|----------|
| **胜利 WIN** | 击毁敌方坦克数 = 敌方总数（默认 4） |
| **失败 LOSE** | ① 玩家 HP ≤ 0；② 弹药 = 0 且无己方炮弹在飞行且场上仍有存活敌人 |

> 同一帧内若胜利与失败条件同时满足，判胜利；checkResult 先查 WIN 后查 LOSE。

判定由 GameStateMachine 每帧检查，触发后：

1. 状态切至 `WIN` / `LOSE`，停止输入与 AI 更新（相机可保留运镜）。
2. WIN 触发后立即销毁全部在途炮弹并冻结 AI 开火。
3. 广播事件，UI 模块展示对应界面（见第 8 章）。

---

## 8. UI / HUD 设计（FR-08 / FR-09 / FR-10 / FR-11）

UI 全部使用 **DOM + CSS 叠加层** 实现，位于 WebGL Canvas 之上。设计风格：军事硬朗风（深色 + 装甲灰 + 猩红点缀），字体使用粗黑体。

### 8.1 主页（HOME 状态）

```
┌─────────────────────────────────────────────┐
│           （背景：3D战场环绕展示）             │
│                                             │
│            库尔斯克会战浪尖战场               │  ← 游戏名，大号描边标题
│         KURSK BATTLEFRONT: WAVE CREST        │  ← 英文副标题（可选）
│                                             │
│              [ 开始战斗 ]                    │  ← 主按钮
│              [ 操作说明 ]                    │  ← 键位弹窗
└─────────────────────────────────────────────┘
```

### 8.2 游戏 HUD（PLAYING 状态）

```
┌─────────────────────────────────────────────────────────┐
│ [HP ▓▓▓▓▓▓▓░░░ 78/100]              击毁: 🛡3    弹药: 12/20 │
│                                                         │
│                                                         │
│                      （3D 画面）                          │
│                    +  准星（中心）                         │
│                                                         │
│  [WASD 移动 | 左键开炮 | 右键瞄准镜]        敌方存活: 2/4   │
└─────────────────────────────────────────────────────────┘
```

HUD 组件明细：

| 组件 | 位置 | 实现要点 |
|------|------|----------|
| 血条 | 左上 | 玩家 HP 条 + 数字；受击时血条闪红并抖动；低于 30% 常亮红色警示 |
| 弹药量 | 右上 | "当前/上限(20)"；击杀奖励 +5，不超过上限 20；装填时显示环形进度；空弹匣变红提示"装填中…" |
| 击毁坦克数 | 右上（弹药下方） | 击毁 +1，伴随击杀图标弹出动画 |
| 敌方存活数 | 右下 | "存活 n/4"，同步战场形势 |
| 准星 | 屏幕中心 | 十字准星；开镜时切换为瞄准镜大准星 |
| 操作提示条 | 左下 | 常驻小字键位提示，10 秒后淡出 |

事件 → HUD 更新映射：

| 事件 | HUD 更新 |
|------|----------|
| `shell:fired` | 弹药 -1 |
| `tank:destroyed` | 击毁数 +1 且 弹药 +5 |
| `player:damaged` | 血条更新 |

### 8.3 失败弹窗（LOSE 状态，FR-08）—— 重点需求

```
┌───────────────────────────────────────────────┐
│                  （画面变暗+去饱和）             │
│      ┌─────────────────────────────────┐      │
│      │          ❌ 战败 ❌               │      │
│      │                                 │      │
│      │   若是巅峰留不住，那就重走来时路     │      │  ← 指定文案
│      │                                 │      │
│      │   [ 返回主页 ]   [ 重新挑战 ]     │      │  ← 指定双按钮
│      └─────────────────────────────────┘      │
└───────────────────────────────────────────────┘
```

交互规格（弹窗时机：死亡爆炸后约 1.0s 弹出）：

| 按钮 | 行为 |
|------|------|
| **返回主页** | 销毁本局实体 → 切换到 `HOME` 状态，显示主页 |
| **重新挑战** | 重置全部游戏数据（坦克、弹药、障碍物、HUD）→ 直接进入 `PLAYING` |

弹窗样式：深色半透明毛玻璃卡片，淡入 + 轻微下落动画；文案居中白色大字；两按钮左右并排，悬停高亮。

### 8.4 胜利横幅（WIN 状态，FR-09）

```
┌─────────────────────────────────────────────┐
│                                             │
│  ══════════════ 胜利 ══════════════         │  ← 全屏横幅，从左侧飞入
│                                             │
│        战绩：击毁 4 辆 · 剩余 HP 45           │  ← 附带战绩小字（可选）
│         [ 重新挑战 ]  [ 返回主页 ]            │
└─────────────────────────────────────────────┘
```

横幅从屏幕左侧滑入至中部，金色描边大字"胜利"（textContent 严格等于"胜利"），背景横向拉出红金色缎带，配合胜利音效。操作按钮（重新挑战 / 返回主页）约 2.0s 后浮现。

### 8.5 水印层（FR-11）

- 地面水印：见 5.2（跟随 3D 场景），透明度 ≥0.08。
- 屏幕水印：`position:fixed; inset:0; pointer-events:none`，DOM 平铺多个绝对定位 span（或平铺背景图），文字"浪尖战场"，透明度 ≥0.08，字号 ≥16px，旋转 -30°，间距 300×200px，不遮挡任何交互。

可见性要求：地面层与屏幕层水印透明度均 ≥0.08，屏幕层字号 ≥16px，保证可辨识但不干扰游玩。四状态水印显隐规则（建议全程显示）：

| 状态 | 地面水印 | 屏幕水印 |
|------|----------|----------|
| HOME | 显示 | 显示 |
| PLAYING | 显示 | 显示 |
| WIN | 显示 | 显示 |
| LOSE | 显示 | 显示 |

---

## 9. 操作反馈系统（FR-13）

| 触发 | 视觉反馈 | 听觉反馈 | 手感反馈 |
|------|----------|----------|----------|
| 开炮 | 炮口火光 + 炮管后坐 + 震屏 | 炮击轰鸣 | 鼠标无反馈（键盘相机震动） |
| 装填完成 | 准星外圈亮起 + "装填完毕"轻提示 | 清脆咔哒声 | — |
| 命中敌人 | 命中处火花 + 敌人闪白 | 金属撞击声 | 命中标记（Hit Marker，准星四角短亮） |
| 击毁敌人 | 爆炸火球 + 浓烟 + 残骸 | 爆炸声 + 低沉轰鸣 | 击杀图标弹出 + 屏幕轻微震动 |
| 玩家受击 | 屏幕边缘红晕 + 血条抖动 | 内部闷响 | 强震屏 |
| 玩家低血量(≤30%) | 血条红闪 + 屏幕持续微红边 | 警报哔声（间隔） | — |
| 碰撞障碍 | 火花 + 坦克急停 | 金属刮擦 | — |
| 燃料桶爆炸 | 大范围爆炸粒子 | 连环爆炸 | 范围内坦克受震 |

---

## 10. 数据结构定义

### 10.1 核心类

```javascript
// 配置
const CONFIG = {
  battlefield: { width: 100, length: 200 },   // 1:2
  player: { hp: 100, speed: 12, ammo: 20, maxAmmo: 20, damage: 34, reload: 2.5 },
  enemy : { count: 4, hp: 60, eliteHp: 100, // 精英血量
            speed: 8, damage: 25, reload: 3.5, eliteReload: 2.5,
            engageRange: 120, keepDistance: 25, combatRange: 30 },
  shell : { speed: 80, radius: 0.25, lifetime: 4, maxDistance: 300 },
};

// 坦克实体
class Tank {
  id; team;                 // 'player' | 'enemy'
  hp; maxHp; ammo;          // 状态
  group;                    // Three.js 模型组
  turret; barrel;           // 炮塔/炮管引用
  collider;                 // AABB {min, max}
  reloadTimer;              // 装填剩余时间
  isAlive;
  takeDamage(dmg, source) { /* 扣血→广播事件→死亡处理 */ }
  fire() { /* 生成炮弹 */ }
}

// 游戏状态（单例）
const Game = {
  state: 'HOME',            // HOME | PLAYING | WIN | LOSE
  kills: 0,                 // 击毁坦克数
  enemiesAlive: 4,
  player: PlayerTank,
  enemies: EnemyTank[],
  obstacles: Obstacle[],
  shells: Projectile[],
  startBattle() {}, restart() {}, backHome() {},
};
```

### 10.2 事件总线（EventBus）

| 事件名 | 载荷 | 订阅者 |
|--------|------|--------|
| `shell:fired` | 位置、方向 | 特效/音效 |
| `shell:hit:tank` | 坦克、伤害 | HUD、特效、胜负判定 |
| `tank:destroyed` | 被毁坦克、凶手 | HUD（击杀数）、胜负判定 |
| `player:damaged` | 伤害值 | HUD、反馈 |
| `game:win` / `game:lose` | 战绩数据 | UI 模块 |
| `ui:button` | 按钮ID | 状态机 |

---

## 11. 主循环与性能

### 11.1 帧循环（requestAnimationFrame）

```
loop(dt):
  if state === PLAYING:
    InputSystem.update()          // 读输入
    PlayerTank.update(dt)         // 玩家移动/炮塔
    AISystem.update(dt)           // 敌方决策(0.2s节流)+移动
    WeaponSystem.update(dt)       // 装填计时
    Projectile.updateAll(dt)      // 炮弹飞行
    CollisionSystem.update(dt)    // 碰撞检测+响应
    GameStateMachine.checkResult()// 胜负判定
  CameraSystem.update(dt)         // 相机跟随(任何状态都跑)
  FeedbackSystem.update(dt)       // 粒子/震屏衰减
  renderer.render()               // 渲染
```

### 11.2 性能优化要点

- 坦克/障碍物模型用共享 Geometry 与 Material（实例化），敌人模型 clone 复用。
- 粒子使用 `THREE.Points` 或对象池，禁止每帧 new。
- 阴影仅对坦克与大型障碍物投射，地面接收。
- AI 决策 0.2s 节流分帧执行。
- DOM HUD 仅在数值变化时更新（脏标记），避免每帧重排。
- 目标 DrawCall < 100。

---

## 12. 游戏流程总览

```
主页"库尔斯克会战浪尖战场"
   │ 点击开始战斗
   ▼
加载场景：生成 1:2 战场(含水印地面) + 障碍物 + 玩家坦克 + 4 辆敌坦克
   ▼
PLAYING：WASD移动 / 鼠标瞄准 / 左键射击 / 右键开镜
   │  HUD 实时显示：血条 | 弹药 | 击毁数
   ▼
┌────────────────────────────┬─────────────────────────────┐
│ 击毁全部4辆敌坦克           │ 玩家HP=0 或 弹尽(无己方炮弹  │
│                            │ 在飞)且仍有敌人              │
├────────────────────────────┼─────────────────────────────┤
│ 胜利横幅"胜利"              │ 弹窗"若是巅峰留不住，          │
│ [重新挑战][返回主页]        │ 那就重走来时路"               │
│                            │ [返回主页][重新挑战]          │
└────────────────────────────┴─────────────────────────────┘
```

---

## 13. 测试要点清单

| 用例 | 预期 |
|------|------|
| 坦克撞墙/障碍物 | 无法穿过，被平滑推出 |
| 炮弹打岩石 | 火花特效，岩石无损 |
| 炮弹打木箱 | 木箱碎裂消失，炮弹消失 |
| 燃料桶被击中 | 爆炸并对附近坦克造成范围伤害 |
| 击毁第 4 辆敌坦克 | 立即弹出胜利横幅，敌方炮弹清场 |
| 玩家被击至 HP=0 | 立即弹出失败弹窗，含指定文案与双按钮 |
| 弹药耗尽时击毁最后一辆 | 判胜利（最后一发命中优先于弹尽判负） |
| 弹药 0、无己方炮弹在飞行且仍有敌人 | 弹出失败弹窗 |
| 点击"重新挑战" | 所有实体/数值重置，直接开局 |
| 点击"返回主页" | 回到主页标题界面 |
| 开镜/关镜 | FOV 平滑切换，灵敏度变化，遮罩 UI 正确 |
| 水印 | 地面透视水印与屏幕水印均显示"浪尖战场" |
| 战场比例 | 实测场景宽:长 = 1:2，边界墙正确合围 |

---

## 附录 A：默认数值配置汇总

| 项 | 值 |
|----|-----|
| 战场尺寸 | 100 × 200（1:2） |
| 玩家 HP / 弹药 | 100 / 20（上限 20，击杀 +5 不超上限） |
| 玩家伤害 / 装填 | 34 / 2.5s |
| 敌人数量 / HP | 4（3普+1精英）/ 60（精英100） |
| 敌人伤害 / 装填 | 25 / 3.5s（精英装填 2.5s） |
| 击毁敌人所需炮弹 | 2 发（普）/ 3 发（精英） |
| 玩家被击毁所需 | 4 发 |

## 附录 B：术语表

| 术语 | 含义 |
|------|------|
| HUD | Heads-Up Display，抬头显示（游戏内界面） |
| AABB | Axis-Aligned Bounding Box，轴对齐包围盒 |
| FOV | Field of View，视场角 |
| EventBus | 事件总线，模块间解耦通信机制 |
| Hit Marker | 命中标记，命中敌人时准星处的反馈提示 |
